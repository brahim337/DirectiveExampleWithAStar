import { SternMakeItBigDirective } from './stern-make-it-big.directive';

describe('SternMakeItBigDirective', () => {
  it('should create an instance', () => {
    const directive = new SternMakeItBigDirective();
    expect(directive).toBeTruthy();
  });
});
